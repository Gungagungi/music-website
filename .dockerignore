# What follows must never enter the build context: either it is rebuilt inside
# the image, or it is dead weight, or — for `.env` — it is a secret that has no
# business in a layer.

node_modules
**/node_modules

app/.next
app/out
app/dist
*.tsbuildinfo

.env
.env.*
!.env.example

.git
.github
.claude

docs
reports
perf
e2e/test-results
e2e/playwright-report
e2e/blob-report
e2e/reports
e2e/.auth
e2e/.cache

Dockerfile
.dockerignore
docker-compose*.yml
Caddyfile
README.md
CLAUDE.md
LICENSE
