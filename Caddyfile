# Reverse proxy et terminaison TLS.
#
# Caddy obtient et renouvelle le certificat tout seul dès que FRETLINE_DOMAIN est
# un nom de domaine public pointant vers ce serveur. Pour un essai local, mettre
# FRETLINE_DOMAIN=:80 dans .env : Caddy sert alors en clair sans rien demander à
# Let's Encrypt.
#
# Traefik ferait la même chose, mais en exposant le socket Docker au proxy ; ça ne
# se rentabilise qu'à partir d'une dizaine de services à découvrir dynamiquement.

{$FRETLINE_DOMAIN} {
	encode zstd gzip

	header {
		# HSTS : Caddy sert déjà en HTTPS, autant interdire la version en clair
		# pour les visites suivantes.
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		# Pas de X-Frame-Options : `frame-ancestors` le remplace et couvre plus
		# de cas.
		#
		# Pas de Content-Security-Policy non plus, et c'est délibéré : la
		# politique complète est posée par l'application (app/src/proxy.ts),
		# parce qu'elle porte un nonce recalculé à chaque requête. Deux en-têtes
		# du même nom ne se remplacent pas — le navigateur applique
		# l'intersection des deux — et un `frame-ancestors 'none'` figé ici
		# passerait inaperçu jusqu'au jour où il contredirait l'autre.
		-Server
	}

	reverse_proxy app:3000 {
		health_uri /api/health
		health_interval 15s
	}

	log {
		output stdout
		format console
	}
}

# Interface Matomo, sur son propre nom de domaine.
#
# Un sous-domaine dédié plutôt qu'un chemin sous le site : Matomo est une
# application PHP complète, qui réécrit ses propres URL et pose ses cookies de
# session sur la racine. La monter sous /analytics obligerait à réécrire ses
# liens un par un, et le premier oubli se solderait par une page blanche.
#
# Variable distincte de FRETLINE_DOMAIN, et non `analytics.{$FRETLINE_DOMAIN}` :
# un essai local met FRETLINE_DOMAIN=:80, et `analytics.:80` n'est pas une
# adresse. Le défaut ci-dessous sert exactement ce cas — Caddy écoute alors en
# clair sur 8081, sans rien demander à Let's Encrypt.
{$FRETLINE_ANALYTICS_DOMAIN::8081} {
	encode zstd gzip

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		Content-Security-Policy "frame-ancestors 'none'"
		-Server
	}

	reverse_proxy matomo:80

	log {
		output stdout
		format console
	}
}

# Serveur de notifications ntfy, destinataire des alertes de l'audit de sécurité.
#
# Même motif que le bloc Matomo : sous-domaine dédié, et défaut en clair sur
# 8082 pour l'essai local, où le port reste sur la boucle locale.
{$FRETLINE_NTFY_DOMAIN::8082} {
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		Content-Security-Policy "frame-ancestors 'none'"
		-Server
	}

	# Pas de `encode` ici, contrairement aux deux autres blocs : la souscription
	# ntfy est un flux d'événements qui reste ouvert, et une couche de
	# compression tamponne — le message n'arrive alors qu'une fois le tampon
	# plein, c'est-à-dire à la prochaine alerte.
	reverse_proxy ntfy:80 {
		flush_interval -1
	}

	log {
		output stdout
		format console
	}
}

# Pré-production : la branche déployée par scripts/deployer-preprod.sh.
#
# Servie par ce Caddy plutôt que par un second : 80 et 443 sont déjà pris, et
# les certificats sont ici. L'application vit dans une autre pile Compose
# (docker-compose.preprod.yml), jointe par le réseau `fretline-edge` sous l'alias
# `preprod-app`. Pré-prod éteinte, ce bloc répond 502 et rien d'autre ne bouge.
#
# Même motif que Matomo et ntfy pour le défaut : en clair sur 8083, sur la boucle
# locale, sans rien demander à Let's Encrypt.
{$FRETLINE_PREPROD_DOMAIN::8083} {
	encode zstd gzip

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		# Une pré-prod indexée finit en résultat de recherche à côté de la
		# boutique, avec ses paniers de test et ses comptes semés.
		X-Robots-Tag "noindex, nofollow"
		-Server
	}

	# Exemption par clé, pour la suite Playwright lancée contre la pré-prod. Le
	# basic auth occupe l'en-tête `Authorization`, que la suite API remplit déjà
	# avec son `Bearer` : les deux ne peuvent pas cohabiter sur une même requête.
	#
	# Une expression plutôt qu'un matcher `header` : sans PREPROD_ACCESS_KEY, la
	# substitution donne une chaîne vide, et un en-tête vide ou absent ne doit
	# jamais passer pour la bonne clé. Le `!= ""` est ce qui ferme cette porte.
	@verrouille not expression `{header.X-Fretline-Preprod} != "" && {header.X-Fretline-Preprod} == "{$PREPROD_ACCESS_KEY}"`

	# Défauts verrouillés, et non vides : un hash vide ferait refuser toute la
	# configuration — et tomber avec elle la boutique, servie par ce même Caddy.
	# Le défaut est le hash d'une chaîne tirée au hasard puis jetée : Caddy
	# démarre (CI, essai local) et personne n'entre.
	basic_auth @verrouille {
		{$PREPROD_BASIC_AUTH_USER:fretline} {$PREPROD_BASIC_AUTH_HASH:$2a$14$Bcoh63tinCQaAAH8qPR4teL49GuNTsmWW1dhmSueQ5xHWLqKMKI6e}
	}

	reverse_proxy preprod-app:3000

	log {
		output stdout
		format console
	}
}
